# Extra deps to run ``python -m src.train`` from ``.venv-maniskill`` (no ``uv run``).
# Install after ``requirements.txt`` (that file already includes loguru, tensorboard, wandb[media]).
# Does not pin ``gymnasium`` — ``mani-skill`` already does.
#
#   pip install -r scripts/maniskill/requirements.txt
#   pip install -r scripts/maniskill/requirements_icl_train.txt
#   export PYTHONPATH=/path/to/icl_adaptation
#
# Avoid ``pip install -e .`` in this venv unless you use ``--no-deps`` and satisfy deps manually,
# or the project may try to pull a second Gym stack.

hydra-core>=1.3.0
omegaconf>=2.3.0
rich>=13.0.0
tqdm
einops
opencv-python-headless>=4.5.0
transformers>=4.20.0
imageio[ffmpeg]>=2.9.0
